# OpenAI API key with Realtime API access
OPENAI_API_KEY=

# Port the Fastify server listens on
PORT=5050

# n8n webhook URL — receives captured leads (writes the Google Sheet row + sends the SMS confirmation)
N8N_WEBHOOK_URL=
